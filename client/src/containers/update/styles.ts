import styled from 'styled-components';

export const StyledUpdateContainer = styled.div`
  display: flex;
  align-items: center;
  flex-direction: column;
  margin-top: 200px;
  position: relative;
`;

export const StyledTitle = styled.h1`
  text-transform: uppercase;
  font-weight: bold;
  letter-spacing: 0.2rem;
  margin-bottom: 30px;
`;
